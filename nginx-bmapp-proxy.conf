# Nginx configuration for BM-APP proxy
# Add this to your nginx server block on 103.105.55.136

# BM-APP API Proxy
location /bmapp-api/ {
    # Target: BM-APP device
    proxy_pass http://103.75.84.183:2323/;

    # Headers
    proxy_set_header Host $http_host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;

    # WebSocket support (for /bmapp-api/video/)
    proxy_http_version 1.1;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection "upgrade";

    # Timeouts
    proxy_connect_timeout 60s;
    proxy_send_timeout 60s;
    proxy_read_timeout 60s;

    # Disable buffering for WebSocket
    proxy_buffering off;

    # CORS headers (if needed)
    add_header Access-Control-Allow-Origin *;
    add_header Access-Control-Allow-Methods "GET, POST, OPTIONS";
    add_header Access-Control-Allow-Headers "Content-Type, Authorization";
}

# Example full server block:
#
# server {
#     listen 4222;
#     server_name 103.105.55.136;
#
#     # Angular app
#     root /path/to/ng-hse-monitoring/dist/ng-hse-monitoring/browser;
#     index index.html;
#
#     # SPA routing
#     location / {
#         try_files $uri $uri/ /index.html;
#     }
#
#     # BM-APP Proxy
#     location /bmapp-api/ {
#         proxy_pass http://103.75.84.183:2323/;
#         proxy_set_header Host $http_host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_http_version 1.1;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection "upgrade";
#         proxy_buffering off;
#     }
# }
